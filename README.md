# ShenaiSDK

Swift Package Manager distribution for the Shen.AI iOS SDK.

Add this repository URL in Xcode or `Package.swift` and select the required release version.
Release tags use bare semantic versions, for example `3.1.1`.
